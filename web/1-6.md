# 📚 1-6 JavaScript 기초

> **학습 목표**: JavaScript의 자료형, 연산자, 조건문, 반복문, 함수, 배열/객체를 이해한다.

---

## 1. 자료형

### 숫자형 (number)
```javascript
let a = 10;    // 정수
let b = 3.14;  // 소수
// JavaScript는 int/float 구분 없이 모두 number!
```

### 문자열 (string)
```javascript
let name = "Alex";
console.log(`Hello, ${name}`);  // 템플릿 리터럴 (백틱 + ${})
```

### 불리언 (boolean)
```javascript
let isAdult = true;
let hasTicket = false;
```

### null vs undefined

| 구분 | 의미 | 예시 |
|:---:|-----|-----|
| undefined | 값 할당 안 됨 | `let score;` |
| null | 의도적으로 비어있음 | `let user = null;` |

---

## 2. 연산자

### 비교 연산자
```javascript
console.log(10 > 5);   // true
console.log(10 >= 10); // true
```

### 동등 연산자

| 연산자 | 의미 | 예시 | 결과 |
|:---:|-----|-----|:---:|
| `==` | 값만 비교 | `10 == "10"` | true |
| `===` | 값+타입 비교 | `10 === "10"` | false |

> ⚠️ 항상 `===` 사용 권장!

### 논리 연산자

| 연산자 | 의미 | 설명 |
|:---:|:---:|-----|
| `&&` | AND | 둘 다 참이어야 참 |
| `\|\|` | OR | 하나만 참이어도 참 |
| `!` | NOT | 반대로 (참↔거짓) |

---

## 3. 조건문

### 계산기 실습
```javascript
let a = 10, b = 5, operator = "+";

if (operator === "+") {
    console.log(a + b);
} else if (operator === "-") {
    console.log(a - b);
} else if (operator === "*") {
    console.log(a * b);
} else if (operator === "/") {
    console.log(a / b);
} else {
    console.log("지원하지 않는 연산자");
}
```

---

## 4. 반복문

### for문
```javascript
for (let i = 0; i < 3; i++) {
    console.log("hello");  // 3번 출력
}
```

| 구성요소 | 역할 | 예시 |
|:---:|-----|-----|
| 초기값 | 시작 값 설정 | `let i = 0` |
| 조건 | 반복 조건 | `i < 3` |
| 증가식 | 1회 반복 후 실행 | `i++` |

### while문
```javascript
let i = 0;
while (i < 3) {
    console.log(i);
    i++;  // ⚠️ 없으면 무한루프!
}
```

---

## 5. 함수

```javascript
function add(a, b) {
    return a + b;
}
let result = add(3, 5);  // 8
```

| 구성요소 | 역할 |
|:---:|-----|
| `function` | 함수 선언 키워드 |
| `add` | 함수 이름 |
| `(a, b)` | 매개변수 |
| `return` | 결과 반환 |

---

## 6. 배열과 객체

### 배열 순회

| 방법 | 코드 | 특징 |
|-----|-----|-----|
| for문 | `for (let i = 0; i < arr.length; i++)` | 인덱스 사용 가능 |
| for...of | `for (const item of arr)` | 간결함, **추천** |
| forEach | `arr.forEach(item => ...)` | break 불가 |

### 객체
```javascript
let user = { name: "Alex", age: 20 };

console.log(user.name);    // 점 표기법
console.log(user["age"]);  // 괄호 표기법
```

---

## 🎓 핵심 정리

| 개념 | 핵심 포인트 |
|-----|---------|
| 자료형 | number, string, boolean, null, undefined |
| 동등 비교 | `===` 사용 (타입까지 비교) |
| 논리 연산자 | `&&` AND, `\|\|` OR, `!` NOT |
| for문 | `for (초기값; 조건; 증가식)` |
| 배열 순회 | `for...of` 추천 |
| 객체 접근 | `obj.key` 또는 `obj["key"]` |

### 📌 코드 흐름

| 순서 | 단계 | 설명 |
|:---:|-----|-----|
| 1 | 변수 선언 | `let`, `const` |
| 2 | 연산 | 산술, 비교, 논리 |
| 3 | 조건문 | `if / else if / else` |
| 4 | 반복문 | `for`, `while` |
| 5 | 함수 | `function 이름() { return }` |
| 6 | 데이터 구조 | 배열 `[]`, 객체 `{}` |
