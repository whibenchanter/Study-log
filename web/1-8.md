# 📚 1-8 JavaScript 화살표 함수와 이벤트

> **학습 목표**: 화살표 함수 문법과 타이머 함수, DOM 이벤트 처리를 이해한다.

---

## 📋 파일 요약

| 파일 | 핵심 내용 |
|-----|---------|
| arrow.js | 화살표 함수 문법 |
| mouseover2.html | 마우스 이벤트 |
| timer.html | setInterval/clearInterval |
| practice6.html | 조건문 + 입력값 처리 |

---

## 1️⃣ arrow.js - 화살표 함수

### 💡 왜 배우는가?
> `function()` 키워드를 `=>`로 줄인 **단축 문법**이다.

### 일반 함수 vs 화살표 함수

| 구분 | 일반 함수 | 화살표 함수 |
|-----|---------|-----------|
| 문법 | `function() { }` | `() => { }` |
| 이름 | 있거나 없음 | 항상 익명 |
| 사용 | 어디든 | 콜백, 이벤트 리스너 |

```javascript
// 일반 함수
button.addEventListener("click", function() {
    console.log("클릭됨");
});

// 화살표 함수 (동일 동작)
button.addEventListener("click", () => {
    console.log("클릭됨");
});
```

---

## 2️⃣ mouseover2.html - 마우스 이벤트

### 💡 왜 배우는가?
> 마우스 호버 효과는 UI 인터랙션의 기본이다.

```html
<div id="box" style="background-color: orange;">마우스를 올려보세요.</div>

<script>
    const box = document.querySelector("#box");

    box.addEventListener("mouseover", () => {
        box.style.backgroundColor = "skyblue";  // 올리면 하늘색
    });
    
    box.addEventListener("mouseout", () => {
        box.style.backgroundColor = "orange";   // 빼면 오렌지색
    });
</script>
```

---

## 3️⃣ timer.html - 타이머 함수

### 💡 왜 배우는가?
> 카운트다운, 자동 슬라이드 등 **시간 기반 동작**에 필수이다.

| 함수 | 역할 |
|-----|-----|
| `setInterval(fn, ms)` | ms마다 fn 반복 실행 |
| `clearInterval(id)` | 타이머 중지 |

```html
<button id="start">카운트다운 시작</button>
<p id="time"></p>

<script>
    const button = document.querySelector("#start");
    const result = document.querySelector("#time");
    let time = 5;

    button.addEventListener("click", function () {
        result.textContent = time;

        const timerId = setInterval(function () {
            if (time === 1) clearInterval(timerId);  // 중지
            time--;
            result.textContent = time;
        }, 1000);  // 1초마다
    });
</script>
```

---

## 4️⃣ practice6.html - 점수 판정기

### 💡 왜 배우는가?
> 사용자 입력값을 받아 조건에 따라 결과를 표시하는 기본 패턴이다.

```html
<input type="text" id="score" placeholder="점수 입력">
<button id="check">확인</button>
<p id="result"></p>

<script>
    const scoreInput = document.querySelector("#score");
    const button = document.querySelector("#check");
    const result = document.querySelector("#result");

    button.addEventListener("click", function () {
        // Number(): 문자열→숫자 변환 (필수!)
        if (Number(scoreInput.value) >= 60) {
            result.textContent = "합격";
        } else {
            result.textContent = "불합격";
        }
    });
</script>
```

---

## 🎓 핵심 정리

| 개념 | 설명 |
|-----|-----|
| `() => {}` | 화살표 함수 (function의 단축형) |
| `addEventListener` | 요소에 이벤트 연결 |
| `setInterval(fn, ms)` | ms마다 fn 반복 |
| `clearInterval(id)` | 타이머 중지 |
| `Number(str)` | 문자열 → 숫자 변환 |
| `textContent` | 요소의 텍스트 변경 |

### 📌 코드 흐름

| 순서 | 단계 | 설명 |
|:---:|-----|-----|
| 1 | 요소 선택 | `document.querySelector()` |
| 2 | 이벤트 등록 | `addEventListener(이벤트, 함수)` |
| 3 | 이벤트 발생 | click, mouseover 등 |
| 4 | 콜백 실행 | 등록한 함수 실행 |
| 5 | DOM 조작 | style, textContent 변경 |
