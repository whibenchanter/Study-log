# 1.8 JavaScript 학습 예제 정리

## 📚 파일 개요

| 파일 | 주요 학습 내용 |
|------|---------------|
| `arrow.js` | 함수 기초, 화살표 함수 문법 |
| `mouseover2.html` | 마우스 이벤트 + 화살표 함수 실습 |
| `timer.html` | 타이머 함수 (`setInterval`/`clearInterval`) |
| `practice6.html` | 조건문 + 입력값 처리 실습 |

---

## 1. arrow.js - 화살표 함수 학습

**핵심 개념**: 
- `function()` vs `() =>` 비교
- `hello` (함수 자체) vs `hello()` (함수 실행 결과) 차이
- 화살표 함수는 **익명 함수**로만 사용

```javascript
//함수의 기본 구조 = 입력 + 동작 + 반환값
// 화살표 함수 = 입력에서 동작 넘어가는 것을 =>'로 표현, 반환값은 return 키워드로 표현

function hello(name) {
    console.log(name);
    return (undefined) // 안써도 이 두 녀석이 숨어있음. 디폴트 리턴값이라는 것의 정체가 이거였음. 이게 중요 ###########
}                      // 리턴 뒤에 값은 쓰고 안쓰고 하는게 영향이 있구나. 인자만 영향 없는 거였음.
                       // 추가로 return null 쓰면 배웠다시피 null 반환됨.
let result = hello     // hello 와 hello()는 다르다. 
//               // hello는 함수 자체를 가리키는 것 - 얘도 틀린거 그런게 아님. 실행하지않고 다르게 사용가능.
//               // hello()는 함수를 실행한 결과값을 가리키는 것. 함수를 실행한거.


//하나더
function add(a, b) {
    return a + b;
}

add(1, 10);  // 함수실행. return이라는게 반환을 의미하고 위로 돌아가서 들어가므로, 옆의 코드는 11로 바껴서 들어간다.
//add = 함수 그 잡채.



// 어제처럼 이벤트리스너 활용해서 한번 더!
document.querySelector("button").addEventListener("click", function() {
    const input = document.querySelector("#name");
    const value = input.value.trim();

    if (value === "") {
        alert("빈 문자열은 허용되지 않습니다.");
    } else {
        console.log(value);
    }
});

// 위에거랑

// 아래것이 같음. =>는 그냥 function(){}의 축약형태로, 원래도 밑에 함수 따로 선언해서 위에 넣어줄수있었는데, 그거를 더 간단히 쓴것.
document.querySelector("button").addEventListener("click", () => { // 위에 펑션을 위치바꿔서 =>로 대체한것.
    const input = document.querySelector("#name");
    const value = input.value.trim();

    if (value === "") {
        alert("빈 문자열은 허용되지 않습니다.");
    } else {
        console.log(value);
    }
});
//화살표 함수는 항상 익명함수로 사용됨. 이름이 없음. 그래서 변수에 담아서 사용하거나, 위에처럼 이벤트리스너에 바로 넣어서 사용함.
```

---

## 2. mouseover2.html - 마우스 이벤트

**핵심 개념**:
- `mouseover` / `mouseout` 이벤트
- 화살표 함수를 이벤트 핸들러로 사용
- 마우스를 올리면 색상 변경 (orange ↔ skyblue)

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div id="box" style="background-color: orange; width: 150px;">마우스를 올려보세요.</div>

    <script>
        const box = document.querySelector("#box");

        // box.addEventListener("mouseover", function () {
        //     box.style.backgroundColor = "skyblue"
        // });
        // box.addEventListener("mouseout", function () {
        //     box.style.backgroundColor = "orange"
        // });

        box.addEventListener("mouseover", () => {
            box.style.backgroundColor = "skyblue"
        });
        box.addEventListener("mouseout", () => {
            box.style.backgroundColor = "orange"
        });
    </script>
</body>
</html>
```

---

## 3. timer.html - 카운트다운 타이머

**핵심 개념**:
- `setInterval()` - 1초마다 반복 실행
- `clearInterval()` - 타이머 종료
- 이벤트 리스너 (`addEventListener`)

```html
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>카운트다운</title>
</head>
<body>
    <button id='start'>카운트다운 시작</button>
    <p id="time"></p>

   
   <script>
    const button = document.querySelector("#start")
    const result = document.querySelector("#time")

    let time = 5;

    button.addEventListener("click", function () {
        result.textContent = time;

        const timerId = setInterval(function () {
            if (time === 1) {
                clearInterval(timerId);
            } //

            time--;
            result.textContent = time;
        }, 1000)
    })
</script>
  
</body>
</html>
```

---

## 4. practice6.html - 점수 판정기

**핵심 개념**:
- `Number()` - 문자열을 숫자로 변환
- 조건문 (`if-else`)
- DOM 조작 (`textContent`)
- 60점 이상이면 "합격", 미만이면 "불합격"

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <input type="text" id="score" placeholder="점수 입력" autocomplete="off">
    <button id="check">확인</button>
    <p id="result"></p>

    <script>
        const scoreInput = document.querySelector("#score")
        const button = document.querySelector("#check")
        const result = document.querySelector("#result")

        button.addEventListener("click", function () {
            //60점 이상이면 result에 합격, 미만이만 불합격
            // "100" 문자열 1 이라는 숫자가 "60"의 6보다 앞이어서 6이 1보다 더 커서 
            if (Number(scoreInput.value) >= 60) {
                result.textContent = "합격";
            } else {
                result.textContent = "불합격";
            }
        });
    </script>
</body>
</html>
```
