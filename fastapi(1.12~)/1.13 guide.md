# 🎮 FastAPI 기초 정복
# 📖 이야기의 시작

상상해봐! 너는 **온라인 쇼핑몰** 사장이야. 🏪

손님들이 우리 쇼핑몰에서:
- 상품 보기
- 상품 등록하기
- 회원가입하기

를 할 수 있어야 해. 이걸 만들어주는 게 바로 **FastAPI**야!

---

# 🧩 1단계: 타입힌트 (type_hints.py)

## 📝 코드

```python
users = [
    {"id": 1, "email": "alex@gmail.com"},
    {"id": 2, "email": "bob@gmail.com"},
    {"id": 3, "email": "chris@gmail.com"},
]
```

## 🎮 비유로 이해하기

이건 마치 **연락처 앱**이야!

```
┌─────────────────────────────────────┐
│          📱 연락처 앱               │
├─────────────────────────────────────┤
│  1번 │ alex@gmail.com              │
│  2번 │ bob@gmail.com               │
│  3번 │ chris@gmail.com             │
└─────────────────────────────────────┘
```

## 🔍 한줄한줄 해석

```python
users = [  # users라는 이름의 목록(리스트)을 만들어!
    {"id": 1, "email": "alex@gmail.com"},  # 1번 사용자
    # ↑ 중괄호 {} = 딕셔너리 (이름표 붙은 상자)
    # "id": 1 → "번호"가 1이야
    # "email": "alex@..." → "이메일"이 alex@...야
]  # 목록 끝!
```

## 💡 핵심 포인트

- `[ ]` = 리스트 (목록, 여러 개 담는 가방)
- `{ }` = 딕셔너리 (이름표 붙은 상자들)
- `"id": 1` = "id"라는 이름표에 1이라는 값

---

# 🧩 2단계: Pydantic이 뭐야? (pydantic_practice.py)

## 🎯 한 문장 요약
> Pydantic = **"데이터 검사관"** 👮

## 📝 코드

```python
from pydantic import BaseModel

class Item(BaseModel):
    name: str
    price: int
```

## 🎮 비유로 이해하기: 놀이공원 입장!

```
┌─────────────────────────────────────────────────────────┐
│                    🎢 놀이공원 입구                      │
│                                                         │
│   👮 경비원 (Pydantic)                                   │
│   "티켓이랑 나이 확인합니다~"                             │
│                                                         │
│   ✅ 티켓 있음 + 나이 숫자 → 입장!                       │
│   ❌ 티켓 없음 or 나이가 "열살" → 입장 불가!              │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## 🔍 한줄한줄 해석

```python
from pydantic import BaseModel
# pydantic이라는 도구 상자에서 BaseModel을 꺼내와!
# BaseModel = 검사 규칙을 만드는 틀

class Item(BaseModel):
# "Item"이라는 검사 규칙을 만들거야!
# BaseModel을 상속받아서 검사 기능을 가져와

    name: str
    # name(이름)은 str(문자열/글자)만 가능해!
    
    price: int
    # price(가격)는 int(정수/숫자)만 가능해!
```

## 🧪 실험해보기

```python
# ✅ 올바른 데이터 → 통과!
item1 = Item(name="사과", price=1000)
print(item1.name)   # 출력: 사과
print(item1.price)  # 출력: 1000

# ❌ 잘못된 데이터 → 에러!
item2 = Item(name="사과", price="천원")
# 💥 에러! price는 숫자여야 하는데 "천원"은 글자잖아!
```

## 💡 왜 필요해?

```
상황: 쇼핑몰에 상품 등록

😱 Pydantic 없으면:
   - 가격에 "무료", "나중에", "???" 같은 거 들어옴
   - 프로그램 터짐!

😊 Pydantic 있으면:
   - "가격은 숫자만!" 하고 미리 막아줌
   - 프로그램 안전!
```

---

# 🧩 3단계: Request Body (request_body.py)

## 🎯 한 문장 요약
> Request Body = **"주문서"** 📋

## 📝 전체 코드

```python
from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI()

class ItemCreateRequest(BaseModel):
    name: str
    price: int

@app.post("/items")
def create_item_api(body: ItemCreateRequest):
    return {"name": body.name, "price": body.price}
```

## 🎮 비유로 이해하기: 식당 주문!

```
┌─────────────────────────────────────────────────────────┐
│                     🍜 라면집                            │
└─────────────────────────────────────────────────────────┘
                         │
        손님이 주문서 작성  │  📋 주문서(Request Body)
                         │  ┌──────────────────┐
                         │  │ 메뉴: 짜장면      │
                         ▼  │ 수량: 2개         │
┌─────────────────────────────────────────────────────────┐
│                                                         │
│   👨‍🍳 주방장 (FastAPI)                                   │
│                                                         │
│   "주문서 확인! 짜장면 2개요~"                            │
│                                                         │
│   📤 응답: {"메뉴": "짜장면", "수량": 2}                  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## 🔍 한줄한줄 해석

```python
from fastapi import FastAPI
# FastAPI 도구 가져오기 (웹서버 만드는 도구)

from pydantic import BaseModel
# Pydantic 도구 가져오기 (데이터 검사 도구)

app = FastAPI()
# "app"이라는 이름의 웹서버를 만들어!
# 이제 이 app에 기능들을 추가할 거야

class ItemCreateRequest(BaseModel):
# "상품 등록 요청서" 양식을 만들어!
    name: str      # 상품 이름 (글자로)
    price: int     # 가격 (숫자로)

@app.post("/items")
# @는 "데코레이터"야 - 아래 함수에 기능을 붙여줘!
# app.post = POST 방식으로 요청받을 때
# "/items" = 주소가 /items일 때
# 즉! "누가 /items 주소로 POST 요청하면 아래 함수 실행해!"

def create_item_api(body: ItemCreateRequest):
# create_item_api라는 함수를 만들어
# body = 손님이 보낸 데이터 (주문서)
# : ItemCreateRequest = 이 양식대로 검사해!

    return {"name": body.name, "price": body.price}
    # body.name = 주문서에서 이름 꺼내기
    # body.price = 주문서에서 가격 꺼내기
    # return = 결과를 손님에게 돌려줘!
```

## 📊 흐름도

```
손님 (클라이언트)                    가게 (서버)
     │                                  │
     │  POST /items                     │
     │  {"name": "사과", "price": 1000} │
     │ ─────────────────────────────▶  │
     │                                  │
     │                          🔍 ItemCreateRequest로
     │                             데이터 검사!
     │                                  │
     │                          ✅ 통과! 함수 실행
     │                                  │
     │  {"name": "사과", "price": 1000} │
     │ ◀─────────────────────────────  │
     │                                  │
```

---

# 🧩 4단계: Response Body (response_body.py)

## 🎯 한 문장 요약
> Response Body = **"응답도 양식대로!"** + **"비밀 정보 숨기기"** 🔒

## 📝 코드

```python
class UserSignUpResponse(BaseModel):
    id: int
    email: str
    # 🔒 password는 여기 없어! → 응답에 안 나감!

@app.post("/users/sign-up", status_code=201)
def sign_up_api(body) -> UserSignUpResponse:
    return {"id": 1, "email": body.email}
```

## 🎮 비유로 이해하기: 명함 만들기!

```
┌─────────────────────────────────────────────────────────┐
│                   회원가입 과정                          │
│                                                         │
│   👤 손님이 보낸 정보:                                   │
│   ┌───────────────────────┐                             │
│   │ 이메일: kim@naver.com │                             │
│   │ 비밀번호: 1234        │ ← 비밀!                     │
│   └───────────────────────┘                             │
│                    │                                    │
│                    ▼                                    │
│   🏪 서버가 돌려주는 정보 (Response):                    │
│   ┌───────────────────────┐                             │
│   │ id: 1                 │                             │
│   │ 이메일: kim@naver.com │                             │
│   │ (비밀번호는 안 줌! 🔒) │ ← 안전!                     │
│   └───────────────────────┘                             │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## 🔍 한줄한줄 해석

```python
class UserSignUpResponse(BaseModel):
# "회원가입 응답" 양식 만들기
    id: int        # 회원번호 (숫자)
    email: str     # 이메일 (글자)
    # ⚠️ password는 없어! 
    # → 밑에서 password를 return해도 여기 없으니까 안 나감!

@app.post("/users/sign-up", status_code=201)
# POST 방식, 주소는 /users/sign-up
# status_code=201 = "새로 만들었어요!" 라는 신호
# (200=성공, 201=새로 생성됨, 404=못찾음, 500=서버에러)

def sign_up_api(body) -> UserSignUpResponse:
#                      ↑ 이 부분!
# -> UserSignUpResponse = "응답은 이 양식으로 줄게!"
# Python이 자동으로 형식 맞춰줌

    return {"id": 1, "email": body.email}
    # 회원번호 1번, 이메일 돌려주기
    # password는 return 안 함! (보안!)
```

## 💡 왜 Response Body가 중요해?

```
❌ Response 양식 없으면:
   return {"id": 1, "email": "...", "password": "1234"}
   → 비밀번호가 그대로 노출! 😱

✅ Response 양식 있으면:
   class Response(BaseModel):
       id: int
       email: str
   → password는 양식에 없으니까 자동으로 제외! 🔒
```

---

# 🧩 5단계: 과제 (과제.py)

## 📝 코드

```python
from fastapi import FastAPI
from pydantic import BaseModel, Field 

app = FastAPI()    

class HelloRequest(BaseModel):
    name: str
    message: str

@app.post("/hello")
def hello_api(body: HelloRequest):
    return {"message": f"안녕하세요, {body.name}님!"}
```

## 🎮 이건 뭘까?

"인사 API"야! 이름을 보내면 인사해주는 기능!

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│   📤 보내는 것 (Request):                               │
│   {"name": "민수", "message": "반가워요"}                │
│                                                         │
│                    ↓                                    │
│                                                         │
│   📥 받는 것 (Response):                                │
│   {"message": "안녕하세요, 민수님!"}                     │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## 🔍 f-string 설명

```python
name = "민수"
message = f"안녕하세요, {name}님!"
# 결과: "안녕하세요, 민수님!"

# f"..." = 중괄호 안에 변수를 넣을 수 있어!
# {name} → 민수 로 바뀜!
```

---

# 🎯 총정리: 전체 흐름

```
┌─────────────────────────────────────────────────────────┐
│                    🌐 FastAPI 쇼핑몰                     │
└─────────────────────────────────────────────────────────┘

 1️⃣ 손님이 요청을 보냄
    POST /items
    {"name": "사과", "price": 1000}
           │
           ▼
 2️⃣ Pydantic이 데이터 검사! 👮
    "name이 글자? ✅ price가 숫자? ✅ 통과!"
           │
           ▼
 3️⃣ 함수가 실행됨 ⚙️
    def create_item_api(body):
        # 로직 처리...
           │
           ▼
 4️⃣ Response Body 형식으로 응답! 📤
    {"name": "사과", "price": 1000}
```

---

# 📚 용어 사전

| 용어 | 쉬운 설명 |
|------|-----------|
| **FastAPI** | 웹서버 만드는 도구 (가게 만들기) |
| **Pydantic** | 데이터 검사관 (경비원) |
| **BaseModel** | 검사 규칙 양식 (신분증 양식) |
| **Request** | 손님이 보내는 것 (주문서) |
| **Response** | 가게가 돌려주는 것 (영수증) |
| **POST** | 새로 만들어달라는 요청 |
| **GET** | 보여달라는 요청 |
| **status_code** | 결과 신호 (201=생성됨, 404=못찾음) |

---

# 🚀 직접 해보기!

```bash
# 1. 터미널에서 서버 실행
cd /Users/admin/FASTAPI/1.13
fastapi dev request_body.py

# 2. 브라우저에서 열기
# http://127.0.0.1:8000/docs

# 3. "Try it out" 클릭해서 테스트!
```
